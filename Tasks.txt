1)	Изучить детально понятие ACID. В частности про уровни изоляции.
2)	Читаем про deadlock
3)	Создать следующие модели для бронирования комнат в отеле и сделать миграции:

class Room(models.Model):
    hotel = models.ForeignKey(Hotel, related_name='rooms', on_delete=models.CASCADE)
    number = models.CharField(max_length=10)
    is_booked = models.BooleanField(default=False)

class Booking(models.Model):
    room = models.ForeignKey(Room, related_name='bookings', on_delete=models.CASCADE)
    start_date = models.DateField()
    end_date = models.DateField()
    customer_full_name = models.CharField(max_length=255)

4)	Создать view и url для сохранения в базу информации о бронировании в отеле.
В параметре пути передать название отеля и id пользователя и номер в отеле
который хочет забронировать( в дальнейшем это будет реализовано через форму).
Получить информацию о пользователе и о отеле.
В случае не существования пользователя или отеля вывести ошибки в консоль.
Далее транзакционно бронируем комнату в отеле,
в случае если данная комната уже забронирована сообщаем
об этом пользователю и откатываем транзакцию.
